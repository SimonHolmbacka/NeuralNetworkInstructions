input_train = Set of instructions to train with
input_target = Set of power values to match the training
input_test = Set of instructions to test with
input_testtarget = Set of power values to match the testing